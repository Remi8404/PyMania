[project]
name = "pmpk"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pandas>=3.0.3",
    "pyopengl>=3.1.10",
    "pyqt6>=6.11.0",
    "pyqtgraph>=0.14.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
